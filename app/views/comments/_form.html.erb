<%= f.fields_for :comments, current_user ? post.comments.build(user_id: current_user.id): post.comments.build do |comment_fields|%>
  <%= comment_fields.text_area :content, :class=> 'text_field new_comment', placeholder: t('.comment')  %>  
<% end %>
  
