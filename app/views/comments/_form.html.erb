<%= f.fields_for :comments, post.comments.build do |comment_fields|%>
  <%= comment_fields.text_area :content, :class=> 'text_field new_comment', placeholder: t('.comment')  %>  
  <%= comment_fields.hidden_field :user_id, value: current_user.id if current_user %>
<% end %>
  
