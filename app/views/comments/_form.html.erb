<%= f.fields_for :comments do |comment_fields|%>
  <%= comment_fields.text_area :content, :class=> 'text_field comment', placeholder: t('.comment') if comment_fields.object.new_record? %>  
<% end %>
