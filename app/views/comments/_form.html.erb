<%= form_for current_user ? current_user.comments.new : Comment.new, :html => { :class => 'form-horizontal'     } do |f|%>
  <%= f.text_area :content, :class=> 'text_field new_comment', placeholder: t('.comment')  %>  
  <%= f.hidden_field :post_id, value: post.id %>
<% end %>
  
