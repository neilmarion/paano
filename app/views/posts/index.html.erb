<%= render "shared/search_field/search_field_holder" %>
<table class="table">
  <thead>
  </thead>
  <tbody>
    <%= render "posts", posts: @posts %>
  </tbody>
</table>
<%= render "shared/pagination", posts: @posts%>
