<div>
  <%= link_to post.type == "Question" ? t('.q') + post.title : t('.a') + post.becomes(Answer).question.title, question_path(post.type == "Question" ? post : post.question), :class => "search_result_link search_result_a" %>
</div>
