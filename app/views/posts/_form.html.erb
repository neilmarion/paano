<% if current_user %>
  <% @question.answers.new %> <!-- for a new answer instance for this question -->
  <%= form_for @question, :html => { :class => 'form-horizontal' } do |f| %>
    <div class="control-group" id="answer_form">
      <div class="controls">
        <%= render 'answers/form', f: f %>  
        <%= f.hidden_field :user_id, value: current_user.id if current_user %>
      </div>
    </div>
    <%= render 'submit', f: f %>
  <% end %>
<% else %>
  <%= link_to t('general.sign_in_with_facebook'), user_omniauth_authorize_path(:facebook), :class => 'btn btn-facebook btn-large' %>
<% end %>

